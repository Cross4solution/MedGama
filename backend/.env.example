# ╔══════════════════════════════════════════════════════════════════╗
# ║  MedGama Backend — Environment Configuration                   ║
# ║  Copy to .env and fill in secrets. NEVER commit .env to git.   ║
# ║                                                                ║
# ║  For Docker: values are injected via docker-compose env_file   ║
# ║  For production: set APP_DEBUG=false, generate APP_KEY          ║
# ╚══════════════════════════════════════════════════════════════════╝

# ── Application ──
APP_NAME=MedGama
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://api.medgama.com
APP_FRONTEND_URL=https://medgama.com

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US
APP_MAINTENANCE_DRIVER=file
APP_TIMEZONE=UTC

# ── Security ──
BCRYPT_ROUNDS=12

# ── Logging ──
LOG_CHANNEL=stack
LOG_STACK=daily
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=warning

# ── Database (PostgreSQL) ──
# Docker: DB_HOST=postgres  |  Local: DB_HOST=127.0.0.1
DB_CONNECTION=pgsql
DB_HOST=postgres
DB_PORT=5432
DB_DATABASE=medgama
DB_USERNAME=medgama
DB_PASSWORD=

# ── Redis (cache + queue + sessions + broadcasting) ──
# Docker: REDIS_HOST=redis  |  Local: REDIS_HOST=127.0.0.1
REDIS_CLIENT=phpredis
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

# ── Session ──
SESSION_DRIVER=redis
SESSION_LIFETIME=120
SESSION_ENCRYPT=true
SESSION_PATH=/
SESSION_DOMAIN=.medgama.com
SESSION_SECURE_COOKIE=true
SESSION_SAME_SITE=lax

# ── Cache & Queue ──
CACHE_STORE=redis
QUEUE_CONNECTION=redis
FILESYSTEM_DISK=local

# ── Broadcasting (Laravel Reverb — self-hosted WebSocket) ──
BROADCAST_CONNECTION=reverb

REVERB_APP_ID=medgama
REVERB_APP_KEY=your-reverb-app-key
REVERB_APP_SECRET=your-reverb-app-secret
REVERB_HOST=0.0.0.0
REVERB_PORT=8080
REVERB_SCHEME=https

# ── Sanctum (API token auth) ──
SANCTUM_STATEFUL_DOMAINS=medgama.com,api.medgama.com

# ── CORS ──
CORS_ALLOWED_ORIGINS=https://medgama.com

# ── Mail (SMTP) ──
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="noreply@medgama.com"
MAIL_FROM_NAME="MedGama"

# ── AWS S3 (optional — for file storage) ──
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=eu-central-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# ── Pusher (alternative to Reverb — cloud WebSocket) ──
# Uncomment and set BROADCAST_CONNECTION=pusher to use Pusher instead of Reverb
# PUSHER_APP_ID=
# PUSHER_APP_KEY=
# PUSHER_APP_SECRET=
# PUSHER_APP_CLUSTER=eu

VITE_APP_NAME="${APP_NAME}"
