# ╔══════════════════════════════════════════════════════════════════╗
# ║  MedGama Backend — Environment Configuration                   ║
# ║  Copy to .env for local dev. For Railway: set env vars in UI.  ║
# ║  NEVER commit .env to git.                                     ║
# ╚══════════════════════════════════════════════════════════════════╝

# ── Application ──
APP_NAME=MedGama
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://your-app.up.railway.app
APP_FRONTEND_URL=https://your-frontend.vercel.app
APP_TIMEZONE=UTC

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US
APP_MAINTENANCE_DRIVER=file

# ── Security ──
BCRYPT_ROUNDS=12

# ── Logging ──
LOG_CHANNEL=stack
LOG_STACK=daily
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=warning

# ── Database (PostgreSQL) ──
# Railway auto-provides DATABASE_URL — Laravel reads it via DB_URL
# If using Railway PostgreSQL plugin, just set:
DB_CONNECTION=pgsql
DATABASE_URL=
# OR use individual vars for local dev:
# DB_HOST=127.0.0.1
# DB_PORT=5432
# DB_DATABASE=medgama
# DB_USERNAME=medgama
# DB_PASSWORD=

# ── Redis ──
# Railway auto-provides REDIS_URL — Laravel reads it natively
REDIS_CLIENT=phpredis
REDIS_URL=
# OR use individual vars for local dev:
# REDIS_HOST=127.0.0.1
# REDIS_PASSWORD=null
# REDIS_PORT=6379

# ── Session ──
SESSION_DRIVER=redis
SESSION_LIFETIME=120
SESSION_ENCRYPT=true
SESSION_PATH=/
SESSION_DOMAIN=null
SESSION_SECURE_COOKIE=true
SESSION_SAME_SITE=none

# ── Cache & Queue ──
CACHE_STORE=redis
QUEUE_CONNECTION=redis
FILESYSTEM_DISK=local

# ── Broadcasting (Laravel Reverb — runs inside same container) ──
BROADCAST_CONNECTION=reverb

REVERB_APP_ID=medgama
REVERB_APP_KEY=
REVERB_APP_SECRET=
REVERB_HOST=0.0.0.0
REVERB_PORT=6001
REVERB_SCHEME=https

# ── Sanctum (API token auth) ──
SANCTUM_STATEFUL_DOMAINS=your-frontend.vercel.app

# ── CORS (Vercel frontend domain) ──
# Comma-separated origins, or * for all
CORS_ALLOWED_ORIGINS=https://your-frontend.vercel.app
# Optional: pattern for Vercel preview deployments
CORS_ALLOWED_PATTERN=https://*-your-team.vercel.app

# ── Mail (SMTP) ──
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="noreply@medgama.com"
MAIL_FROM_NAME="MedGama"

# ── AWS S3 (optional — for file storage) ──
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=eu-central-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"
